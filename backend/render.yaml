services:
  - type: web
    name: placement-app-api
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: MONGODB_URI
        sync: false
      - key: ADMIN_KEY
        sync: false
      - key: PORT
        value: 3001
